<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>hellonaga</artifactId>
        <groupId>naga</groupId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>hellonaga-gwt</artifactId>

    <dependencies>

        <dependency>
            <artifactId>hellonaga-logic</artifactId>
            <groupId>${project.groupId}</groupId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <artifactId>naga-gwt</artifactId>
            <groupId>${project.groupId}</groupId>
            <version>${project.version}</version>
        </dependency>

    </dependencies>

    <properties>
        <!-- Skipping the artifact installation by default (in case the profile is not activated) -->
        <param.install.skip>true</param.install.skip>
    </properties>

    <profiles>
        <!-- profile to transpile the java code into javascript using GWT -->
        <profile>
            <id>gwt</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>

            <properties>
                <!-- Not skipping the artifact installation when the profile is activated -->
                <param.install.skip>false</param.install.skip>
                <!-- Avoiding size consuming META-INF/maven/** coming dependencies (useless for this artifact) -->
                <param.jar.addMavenDescriptor>false</param.jar.addMavenDescriptor>
                <!-- Setting the GWT plugin configuration parameters -->
                <param.gwt.module>hellonaga</param.gwt.module>
                <param.gwt.compileSourcesArtifact1>${project.groupId}:hellonaga-logic</param.gwt.compileSourcesArtifact1>
                <param.gwt.compileSourcesArtifact3>${project.groupId}:naga-core</param.gwt.compileSourcesArtifact3>
                <param.gwt.compileSourcesArtifact2>${project.groupId}:naga-gwt</param.gwt.compileSourcesArtifact2>
            </properties>

            <build>
                <plugins>
                    <!-- Calling the GWT maven plugin to compile the gwt module into javascript -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>gwt-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>